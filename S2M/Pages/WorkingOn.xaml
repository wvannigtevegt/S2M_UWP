<Page
    x:Class="S2M.Pages.WorkingOn"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:S2M.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Loaded="Page_Loaded"
    mc:Ignorable="d">

		<StackPanel x:Name="WorkinOnInputStackPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
			<TextBox x:Name="WorkingOnTextBox" 
					 Header="What is on top of your mind today" 
					 Text="{x:Bind WorkingOnString, Mode=TwoWay}" 
					 HorizontalAlignment="Stretch" 
					 Width="400" />

			<Button x:Name="SaveWorkingOnButton" 
					Content="See matches" 
					Margin="0,20,0,0" 
					HorizontalAlignment="Stretch" 
					Background="Lime" 
					Click="SaveWorkingOnButton_Click" />
		</StackPanel>
</Page>
